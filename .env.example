# Puerto en el que escucha el servicio
PORT=4123

# Token compartido para autenticar requests desde n8n.
# Si lo dejas vacío, el endpoint queda abierto (OK para localhost, NO para deploy público).
# Genera uno largo con: openssl rand -hex 32
RENDERER_AUTH_TOKEN=
